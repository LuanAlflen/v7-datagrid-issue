import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#ff00ff",
        },
        text: {
            primary: "#fff",
            secondary: "#fafafa",
            disabled: "##737373",
        },
    },
});
